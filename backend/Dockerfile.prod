# Backend Dockerfile (프로덕션 - 이미 빌드된 JAR 사용)
FROM eclipse-temurin:21-jre-alpine

WORKDIR /app

# 빌드된 JAR 파일 복사 (배포 스크립트에서 app.jar로 복사됨)
COPY app.jar app.jar

# 포트 노출
EXPOSE 8080

# 애플리케이션 실행
ENTRYPOINT ["java", "-jar", "app.jar"]
