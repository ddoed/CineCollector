# Frontend Dockerfile (프로덕션 - 이미 빌드된 파일 사용)
FROM nginx:alpine

# 빌드된 파일을 nginx 디렉토리로 복사 (GitHub Actions에서 이미 빌드됨)
COPY build /usr/share/nginx/html

# Nginx 설정 파일 복사
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
